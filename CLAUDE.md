CLAUDE.md

이 문서는 이 디렉터리에서 작업할 때 따라야 하는 기본 지침이다. 사용자의 요청을 정확히 이해하고, 필요한 범위 안에서 안전하고 검증 가능한 변경을 수행한다.

기본 원칙

•
기본 응답 언어는 한국어로 한다. 사용자가 다른 언어를 요청하면 그 언어를 사용한다.

•
작업을 시작하기 전에 요청 내용과 관련 파일을 충분히 확인한다.

•
요구사항이 모호하거나 중요한 정보가 부족하면 추측하지 말고 먼저 질문한다.

•
요청을 해결하는 데 필요한 최소 범위만 변경한다.

•
기존 코드 구조, 명명 규칙, 포맷과 설계 의도를 우선 존중한다.

•
관련 없는 리팩터링, 파일 이동, 이름 변경, 의존성 추가는 피한다.

•
단순히 동작하는 코드보다 읽기 쉽고 유지보수 가능한 코드를 작성한다.

작업 절차

1.
요청사항과 현재 프로젝트 상태를 확인한다.

2.
관련 파일, 설정, 문서, 테스트를 먼저 읽는다.

3.
변경 범위와 예상 영향을 정리한다.

4.
기존 스타일을 유지하며 필요한 코드를 수정한다.

5.
가능한 테스트, 타입 검사, 린트, 빌드를 실행한다.

6.
변경 내용과 검증 결과를 사용자에게 간결하게 보고한다.

간단한 수정은 바로 처리할 수 있지만, 여러 파일에 영향을 주거나 위험도가 높은 작업은 구현 전에 계획을 세운다.

코드 작성 규칙

•
의미가 분명한 변수명, 함수명, 파일명을 사용한다.

•
함수와 모듈은 하나의 명확한 책임을 갖도록 작성한다.

•
중복 코드는 합리적인 범위에서 줄이되, 불필요한 추상화는 만들지 않는다.

•
주석은 코드가 무엇을 하는지 반복하기보다 왜 그렇게 구현했는지 설명할 때만 사용한다.

•
오류를 숨기지 말고 적절히 처리하며, 사용자에게 필요한 오류 정보는 이해하기 쉽게 제공한다.

•
입력값과 외부 데이터는 신뢰하지 말고 필요한 검증을 수행한다.

•
기존 타입 시스템을 유지하고, 불필요한 any나 타입 우회는 피한다.

•
하드코딩보다 기존 설정과 상수를 우선 사용한다.

•
새 의존성은 꼭 필요한 경우에만 추가하고, 기존 도구로 해결 가능한지 먼저 확인한다.

•
공개 API나 사용자 동작을 변경할 때는 하위 호환성과 영향 범위를 검토한다.

파일 변경 규칙

•
자동 생성 파일, 빌드 결과물, 의존성 디렉터리는 직접 수정하지 않는다.

•
사용자가 작성한 변경사항을 임의로 되돌리거나 덮어쓰지 않는다.

•
파일 삭제, 대규모 이동, 데이터 마이그레이션은 필요성과 영향을 확인한 뒤 수행한다.

•
설정 또는 실행 방법이 바뀌면 관련 문서와 예제도 함께 갱신한다.

•
비밀정보, 토큰, 비밀번호, 개인키를 코드나 문서에 기록하지 않는다.

•
예시 환경 변수에는 실제 값 대신 명확한 자리표시자를 사용한다.

테스트 및 검증

변경 후 프로젝트에 준비된 검증 명령을 우선 사용한다. 일반적인 확인 순서는 다음과 같다.

1.
포맷 검사

2.
린트

3.
타입 검사

4.
관련 단위 테스트 및 통합 테스트

5.
빌드

모든 명령을 무조건 실행하지 말고 변경 내용과 프로젝트 구성에 맞는 검증을 선택한다. 테스트가 실패하면 원인을 확인하고, 기존 실패인지 이번 변경으로 인한 실패인지 구분한다. 실행하지 않은 검증을 실행했다고 보고하거나 실패를 숨기지 않는다.

Git 규칙

•
현재 변경사항을 확인한 후 작업하며, 사용자 변경을 보존한다.

•
요청받지 않은 커밋, 푸시, 브랜치 생성, 태그 생성은 하지 않는다.

•
reset --hard, 강제 푸시, 대량 삭제 같은 파괴적 명령은 명시적 승인 없이 실행하지 않는다.

•
변경은 작고 집중된 단위로 유지하며, 관련 없는 파일을 함께 수정하지 않는다.

•
커밋을 요청받은 경우 메시지는 변경 목적이 드러나도록 명확하게 작성한다.

보안 및 안전

•
인증정보와 개인정보를 로그, 오류 메시지, 테스트 데이터에 노출하지 않는다.

•
외부 서비스 호출, 실제 메시지 전송, 결제, 배포, 운영 데이터 변경 등 부작용이 있는 작업은 실행 전에 범위와 승인을 확인한다.

•
위험한 입력을 셸 명령, SQL, 템플릿, 동적 코드 실행에 그대로 전달하지 않는다.

•
보안 검사를 비활성화하거나 권한을 확대하는 방식으로 문제를 우회하지 않는다.

•
취약점을 발견하면 악용하지 말고 영향과 안전한 수정 방법을 알린다.

커뮤니케이션

작업 중 중요한 가정이나 제한사항이 있으면 명확히 알린다. 완료 보고에는 다음 내용을 포함한다.

•
무엇을 변경했는지

•
어떤 파일이 변경되었는지

•
어떤 검증을 실행했고 결과가 어땠는지

•
실행하지 못한 검증이나 남아 있는 제한사항이 있는지

설명은 간결하게 작성하되, 사용자가 결과를 확인하고 다음 행동을 결정하는 데 필요한 정보는 빠뜨리지 않는다.

